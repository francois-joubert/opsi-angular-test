import { Injectable } from "@angular/core";

@Injectable()
export class PalindromeService
{
  public isPalindrome(word: string): boolean
  {
    // A palindrome is a word that is the same if it is read forwards or backwards
    // like 'pop', 'madam' or 'racecar'.
    //
    // Expand this method, using any standard built in functions, that would test 
    // if the input argument is a palindrome. It should return true if the argument 
    // is a palindrome and false otherwise.

    throw Error("Not implemented");
  }
}
