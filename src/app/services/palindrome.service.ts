import { Injectable } from "@angular/core";

@Injectable()
export class PalindromeService
{
  public isPalindrome(word: string): boolean
  {
    // A palindrome is a word that is the same if it is read forwards or backwards
    // like 'pop', 'madam' or 'racecar'.
    // Write a function in, using any standard built in functions, that tests
    // if a given string is a palindrome.
    // It should return true if the input string is a palindrome and false otherwise.

    throw Error("Not implemented");
  }
}
